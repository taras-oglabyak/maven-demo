name: run_tests

on: workflow_dispatch

jobs:

  build-job:
    runs-on: ubuntu-latest
    steps:
    - name: Step-1, Hello
      run: |
        echo "hello, kurwa"
        echo "end"
    - name: Print env
      run: printenv
